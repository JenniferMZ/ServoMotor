# ServoMotor
FITEC continuous servo FS90R

This program allows a user to control a servo motor rotation direction (clockwise vs counterclockwise), through a number input in the serial monitor with number 1-90 corresponding to clockwise rotation and numbers 100-180 corresponding to counterclockwise rotation. The loop resets the rotation to 95 (a neutral position), after a 3 second delay.
